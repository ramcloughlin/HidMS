@model IEnumerable<hidMy.Models.BusinessCategoryType>

@{
    ViewBag.Title = "Index";
}

<h2>Business Category/Type 
    Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    @*<tr>
        <th>
            @Html.DisplayNameFor(model => model.BusinessCategory)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.BusinessType)
        </th>
        <th></th>
    </tr>*@

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.BusinessCategory)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.BusinessType)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.CatID }) |
            @Html.ActionLink("Details", "Details", new { id=item.CatID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.CatID })
        </td>
    </tr>
}

</table>
